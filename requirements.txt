numpy
opencv-python
scipy
torch
torchvision
smplx
huggingface_hub
# Multi-HMR estimator deps (source cloned by install.py into vendor/multi-hmr)
einops
roma
pillow
# Multi-HMR2 estimator deps (source cloned by install.py into vendor/multi-hmr2)
anny
warp-lang
scikit-learn
torchmetrics
termcolor
# WiLoR hand estimator deps (source cloned by install.py into vendor/WiLoR)
ultralytics
yacs
timm
pytorch-lightning
# Optional: GLB export in the Export Mesh node
# trimesh
